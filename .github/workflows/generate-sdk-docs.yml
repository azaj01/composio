name: Generate SDK Docs

on:
  push:
    branches: [next]
    paths:
      - 'ts/packages/core/src/**'
      - 'python/composio/**'
  workflow_dispatch:

jobs:
  generate-ts-docs:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v6

      - name: Setup Node.js, pnpm, Bun
        uses: ./.github/actions/setup-node-pnpm-bun

      - run: pnpm install --frozen-lockfile

      - name: Generate TypeScript SDK docs
        working-directory: docs
        run: bun run generate:sdk-reference:ts

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@98357b18bf14b5342f975ff684046ec3b2a07725 # v8.0.0
        with:
          commit-message: 'docs: auto-generate TypeScript SDK reference'
          title: 'docs: Update TypeScript SDK reference'
          body: Auto-generated TypeScript SDK docs.
          branch: docs/auto-update-ts-sdk-reference
          base: next
          add-paths: docs/content/reference/sdk-reference/typescript/

  generate-python-docs:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v6

      - name: Setup Python with UV
        uses: ./.github/actions/setup-python-uv

      - name: Generate Python SDK docs
        working-directory: docs
        run: uv run --with griffe python scripts/sdk-reference/generate-python.py

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@98357b18bf14b5342f975ff684046ec3b2a07725 # v8.0.0
        with:
          commit-message: 'docs: auto-generate Python SDK reference'
          title: 'docs: Update Python SDK reference'
          body: Auto-generated Python SDK docs.
          branch: docs/auto-update-python-sdk-reference
          base: next
          add-paths: docs/content/reference/sdk-reference/python/
